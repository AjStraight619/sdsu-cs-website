import { db } from "@/lib/db";

export default async function HomePage() {
	// const user = await db.user.findUnique({
	// 	where: {
	// 		email: "test@user.com"
	// 	}
	// })
	return (
		<div>

		</div>
	)
}
